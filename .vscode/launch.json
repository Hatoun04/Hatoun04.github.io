{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Current File", // Name the configuration as "Run Current File"
            "type": "chrome",
            "request": "launch",
            "file": "${file}", // This tells VS Code to run the currently open file
            "webRoot": "/Users/hatoun/Documents/GitHub/web project repositry" // Updated web root path
        }
    ]
}
